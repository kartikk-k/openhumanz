/**
 * Application-wide constants.
 */

/** Human readable name of the application. */
export const APP_NAME = 'Assistant';

/** Default workspace root, as shown in the UI before settings resolve it. */
export const DEFAULT_WORKSPACE_HINT = '~/.assistant';
